{{ form_start(form) }}
    <div id="form-new-partner" class="container bg-light rounded">
            <div class="row bg-light border-bottom border-right border-left">
                <div class="col-12 mt-5 pb-4">
                    <div class="col-12 mt-5">
                        <ul class="list-group list-group-flush">
                            <li class="list-group">
                                <div class="row mb-3">
                                    <div class="col-1 p-0 text-center"><i class="fad fa-id-badge"></i></div>
                                    <div class="col-4">{{ form_label(form.label) }}</div>
                                    <div class="col-6 p-0">{{ form_widget(form.label, {'attr': {'placeholder': 'Question'}}) }}</div>
                                </div>
                            </li>
                            <li class="list-group">
                                <div class="row mb-3">
                                    <div class="col-1 p-0 text-center"><i class="fad fa-calendar-check"></i></div>
                                    <div class="col-4">{{ form_label(form.help) }}</div>
                                    <div class="col-6 p-0">{{ form_widget(form.help) }}</div>
                                </div>
                            </li>
                            <li class="list-group">
                                <div class="row mb-3">
                                    <div class="col-1 p-0 text-center"><i class="fad fa-calendar-check"></i></div>
                                    <div class="col-4">{{ form_label(form.sondage) }}</div>
                                    <div class="col-6 p-0">{{ form_widget(form.sondage, {'attr': {'placeholder': 'Sondage'}}) }}</div>
                                </div>
                            </li>
                        </ul>
                    </div>
                </div>
                <div class="col-12 text-right p-4">
                    <a href="{{ path('question_index') }}" class="btn btn-secondary">Annuler</a>
                    <button class="btn btn-style">{{ button_label|default('Valider') }}</button>
                </div>
            </div>
        </div>
{{ form_end(form) }}
