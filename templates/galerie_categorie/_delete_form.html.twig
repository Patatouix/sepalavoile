<form method="post" action="{{ path('galerie_categorie_delete', {'id': galerie_categorie.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ galerie_categorie.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>