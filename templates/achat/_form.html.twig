{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}


{{ form_start(form) }}
    <div id="form-new-achat" class="container bg-light rounded">
        <div class="row bg-light border-bottom border-right border-left">
            <div class="col-12 mt-5 pb-4 px-5">
                <div class="col-12 mt-5">
                    <ul class="list-group list-group-flush">
                        <li class="list-group">
                            <div class="row mb-5">
                                <div class="col-1 col-sm-1 p-0 text-center"><i class="fad fa-inbox-in mr-2"></i></div>
                                <div class="col-11 col-sm-4">{{ form_label(form.medias) }}</div>
                                <div class="col-12 col-sm-7 p-0">{{ form_widget(form.medias) }}</div>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
            <div class="col-12 text-right p-4">
                <a href="{{ path('admin_achat_index') }}" class="btn btn-secondary">Annuler</a>
                <button class="btn btn-style">{{ button_label|default('Valider') }}</button>
            </div>
        </div>
    </div>
{{ form_end(form) }}